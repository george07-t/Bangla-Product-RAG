# Copy this to .env and fill in your values

# Required for LLM responses (OpenAI, or any OpenAI-compatible API)
OPENAI_API_KEY=sk-...

# Optional: use a different OpenAI-compatible endpoint (Groq, Together, etc.)
# OPENAI_BASE_URL=https://api.groq.com/openai/v1

# Server settings (optional)
HOST=0.0.0.0
PORT=8000
